myObj = {
    apple:"red",
    ball:"round",
    cat:"meow",
    dog:"bone"
}

    console.log(myObj.apple + '\n' + myObj.ball + '\n' + myObj.cat + '\n' + myObj.dog );

   // another way
    // var txt = "";
    // for (let y in myObj) {
    // txt += myObj[y] + "\n";
    // }
    // console.log(txt);